enum PopupMenu {
  note_sort,
  note_options,
  category_settings
}

enum NoteSortMenuItem {
  sort_by_time,
  sort_by_title,
  sort_by_category
}

enum NoteOptionsMenuItem {
  export,
  import,
  select_all,
  unselect_all,
  settings
}

enum CategorySettingsMenuItem {
  settings
}